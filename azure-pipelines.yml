# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  jobs:
  - job: Buildjob
    steps:
    - bash: echo "Build please"
- stage: Devdeploy
  jobs:
  - deployment: devdeply job
    environment: Dev
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo deploy to Dev
- stage: QAdeploy
  jobs:
  - deployment: QAdeply job
    environment: QA
    strategy:
     runOnce:
       deploy:
         steps:
         - script: echo deploy to QA
